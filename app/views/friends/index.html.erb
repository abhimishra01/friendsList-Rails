<br />

<table class="table table-striped table-bordered table-hover">

  <thead class="table-dark">
    <tr>
      <th>Name
      </th>
      <th>Email</th>
      <th>Phone</th>
      <th>Twitter</th>
      <th>User ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @friends.each do |friend| %>
    <% if friend.user == current_user%>
  <% if @friends.length == 0 %>
  <h2>No Data available</h2>
  <% else %>
    <tr>
      <td><%= link_to friend.first_name + " " + friend.last_name, friend %></td>
      <td><%= friend.email %></td>
      <td><%= friend.phone %></td>
      <td><%= friend.twitter %></td>
      <td><%= friend.user_id %></td>
      <%# <><%= link_to 'Show', friend, class:"btn btn-primary" %>
      <%# <td><%= link_to 'Edit', edit_friend_path(friend), class:"btn btn-secondary" %>
      <td><%= link_to 'Destroy', friend, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-outline-danger btn-sm" %>
      </td>
    </tr>
    <% end %>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add New Friend', new_friend_path, class:"btn btn-dark"   %>